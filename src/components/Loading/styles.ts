import styled from 'styled-components'

const Loading = styled.div`
  display: flex;
  align-items: center;
  justify-content: center;
  height: 100%;
`

const Styled = { Loading }

export default Styled
